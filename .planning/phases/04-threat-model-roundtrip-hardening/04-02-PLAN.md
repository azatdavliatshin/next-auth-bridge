---
phase: 04-threat-model-roundtrip-hardening
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - docs/threat-model.md
  - CLAUDE.md
autonomous: true
requirements: [HARDEN-01]
must_haves:
  truths:
    - "docs/threat-model.md exists and enumerates every canonical Mode A THREAT-NN invariant as a table keyed by THREAT-NN"
    - "Every row maps property -> mitigation (code evidence) -> a specific test-file::test-name that is currently GREEN (D-03/D-04)"
    - "Every cited test::name resolves to an actually-passing test in the packages/core suite (verified by re-running the suite and grepping cited names)"
    - "The THREAT-06 partitioned-cookie row states the D-11 honesty boundary explicitly (attribute emission + data flow only, NOT real CHIPS enforcement)"
    - "THREAT-01 and THREAT-10 both cite the hardened roundtrip it(...) name produced by Plan 01 (locked, not drifted)"
    - "docs/architecture.md is NOT created (D-07); the invariant registry lives inside docs/threat-model.md"
    - "CLAUDE.md's single docs/architecture.md reference (line ~125) is redirected to docs/threat-model.md; the threat-model-discipline rule (lines ~71/73) is left intact (D-08)"
  artifacts:
    - path: "docs/threat-model.md"
      provides: "Canonical, self-contained Mode A invariant registry: THREAT-NN -> property -> mitigation -> green test citation"
      contains: "THREAT-01"
    - path: "CLAUDE.md"
      provides: "Redirected session-start pointer (docs/threat-model.md), discipline rule intact"
      contains: "docs/threat-model.md"
  key_links:
    - from: "docs/threat-model.md"
      to: "packages/core/src/__tests__/*.test.ts"
      via: "every row cites a green test-file::test-name (D-04)"
      pattern: "test\\.ts"
    - from: "CLAUDE.md"
      to: "docs/threat-model.md"
      via: "session-start pointer redirected from architecture.md"
      pattern: "docs/threat-model\\.md"
---

<objective>
Author the canonical `docs/threat-model.md` (HARDEN-01) — a self-contained, table-keyed registry
of every Mode A security invariant, each THREAT-NN row mapping property -> mitigation (code
evidence) -> a specific `test-file::test-name` that is currently GREEN. The `docs/` directory does
not yet exist; create it. Then redirect CLAUDE.md's single `docs/architecture.md` reference to
`docs/threat-model.md` (D-08), keeping the threat-model-discipline rule intact and NOT creating
`docs/architecture.md` (D-07).

Purpose: This doc is the living traceability artifact (D-04) that binds the security story to the
test suite — the deliverable an external auditor or the future Auth.js docs-recipe reader consults.
Per D-04 a row citing a non-existent or failing test is a HARDEN-01 defect, so every citation must
be verified against the green suite before this plan is done.

Output: `docs/threat-model.md` (new) + a one-line CLAUDE.md pointer edit.

This plan DEPENDS ON 04-01: the hardened roundtrip's `it(...)` names are the THREAT-01 / THREAT-07
(roundtrip facet) / THREAT-10 citations and the new replay + wrong-origin negatives are the
roundtrip-level THREAT-06 / THREAT-03 citations. Read 04-01-SUMMARY.md for the final locked names.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-threat-model-roundtrip-hardening/04-CONTEXT.md
@.planning/phases/04-threat-model-roundtrip-hardening/04-RESEARCH.md
@.planning/phases/04-threat-model-roundtrip-hardening/04-PATTERNS.md
@.planning/phases/04-threat-model-roundtrip-hardening/04-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan (04-02) produces:

- `docs/threat-model.md` (NEW FILE) — the canonical invariant registry. Required content: a table
  keyed by every canonical THREAT-NN (THREAT-01 through THREAT-10, with THREAT-02 split into its
  entropy / one-time-use / TTL facets per RESEARCH Open Question 2). Each row: property ->
  mitigation (code evidence with file:line) -> `test-file :: it-name` (a GREEN test). Plus a short
  prose intro and an explicit D-11 honesty-boundary note on the partitioned-cookie row.
- `CLAUDE.md` — one redirected line (the single `docs/architecture.md` reference -> `docs/threat-model.md`).

The `docs/` directory is created by writing `docs/threat-model.md` into it.

No source/test symbols are produced by this plan (it cites the symbols Plan 01 locked).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Author docs/threat-model.md — the THREAT-NN-keyed invariant registry citing only green tests (HARDEN-01)</name>
  <files>docs/threat-model.md</files>
  <read_first>
    - .planning/phases/04-threat-model-roundtrip-hardening/04-01-SUMMARY.md (the FINAL locked it(...) names for the hardened roundtrip happy-path + replay + wrong-origin/source negatives — cite these verbatim)
    - .planning/phases/04-threat-model-roundtrip-hardening/04-RESEARCH.md (lines 180-199 — the verified THREAT-NN -> file :: it-name map; this is the drop-in row source, every name confirmed green; lines 201-213 the T-03-NN reconciliation; lines 406-417 the STRIDE seed content)
    - .planning/phases/04-threat-model-roundtrip-hardening/04-PATTERNS.md (lines 23-53 — the 02-SECURITY.md row shape to copy: property -> mitigation (file:line) -> test::name; the honesty-boundary row; the THREAT-01/THREAT-10 dual-cite caveat)
    - .planning/phases/02-bridge-consume-routes/02-SECURITY.md (the established in-repo THREAT-NN table form — copy the row shape + file:line + test::name citation discipline; DROP the phase-audit framing: no frontmatter, no CLOSED/OPEN column, no Accepted Risks log)
    - packages/core/src/__tests__/roundtrip.e2e.test.ts (the hardened file from Plan 01 — confirm the exact it(...) strings for THREAT-01/06/07/10 citations match what you write)
    - CLAUDE.md (lines ~63-77 "Threat model discipline" — the seven security-critical invariants summarized; do NOT contradict them)
  </read_first>
  <action>
    Create `docs/threat-model.md` (creating the `docs/` directory). Structure per D-03/D-05/D-07:
    a single self-contained registry, NOT a phase audit.

    Content:
    - A short prose intro (a few sentences): scope = Mode A (popup-bridge) cross-context auth; the
      one architectural shape (server-side handle store mediating a one-time-code exchange across a
      trust boundary); a note that THREAT-NN is the canonical invariant namespace (per
      REQUIREMENTS.md) and that this doc is the single source of truth (D-05/D-07); a line stating
      Mode B is out of scope for v0.1 tests but informs the threat scope.
    - The invariant table keyed by THREAT-NN. Columns (discretion, but MUST include): THREAT-NN |
      Property | Mitigation (code evidence, file:line) | Test (`test-file :: it-name`). Rows for
      every canonical invariant, sourced from RESEARCH lines 184-198:
        - THREAT-01 (full roundtrip) -> cite the hardened roundtrip happy-path it() name (from 04-01-SUMMARY).
        - THREAT-02 entropy / one-time-use / TTL -> THREE sub-rows (RESEARCH Open Question 2),
          citing generate-code.test.ts and in-memory.test.ts names.
        - THREAT-03 (postMessage origin+source) -> cite is-trusted-message.test.ts + open-auth-popup.test.ts
          names AND the new roundtrip wrong-origin/source negative (from 04-01-SUMMARY).
        - THREAT-04 (session-gate-first) -> bridge-route.test.ts names.
        - THREAT-05 (PKCE non-interference / decoy exclusion) -> bridge-route.test.ts harvest name.
        - THREAT-06 (one-time handle; forged/replayed -> 4xx; partitioned cookie) -> consume-route.test.ts
          names AND the new roundtrip replay negative (from 04-01-SUMMARY). This row MUST state the
          D-11 honesty boundary explicitly: the bench proves Partitioned attribute EMISSION + data
          flow ONLY, NOT real CHIPS partition enforcement (that is a manual/browser check). The row
          MUST NOT assert "consume must be a navigation / fetch is a violation": per the D-09 review
          addendum the real-client transport (fetch vs navigation) is an UNRESOLVED empirical CHIPS
          question (Phase 3's "prefer fetch" stands), verified in a real browser in Phase 5 — state it
          as the open transport question, not a settled requirement.
        - THREAT-07 (client URL hygiene) -> popup-flow.test.ts names + the roundtrip sweep (happy-path it).
        - THREAT-08 (sanitizeNext) -> auth-helpers.test.ts names.
        - THREAT-09 (no token in bridge body/URL) -> bridge-route.test.ts opaque-handle name.
        - THREAT-10 (no token in URL at roundtrip level) -> the SAME hardened roundtrip happy-path it
          as THREAT-01 (dual-cite the same name — lock per the THREAT-01/THREAT-10 caveat).

    Every `test-file :: it-name` MUST be an EXACT, currently-green test string. Use the RESEARCH map
    as the verified source and reconcile THREAT-01/03/06/07/10 against the FINAL names in
    04-01-SUMMARY.md / the hardened roundtrip file. NO row may cite a non-existent or failing test
    (D-04 — that is a HARDEN-01 defect).

    Style: no emoji in the doc body (CLAUDE.md — emoji OK only in README headings; this is not the
    README). No per-file SPDX/license header (MIT declared once at root). Plain markdown table(s).
    Do NOT create docs/architecture.md (D-07).
  </action>
  <verify>
    <automated>cd packages/core && pnpm test && cd .. && for t in $(grep -oE '[a-z0-9-]+\.test\.ts :: "[^"]+"' ../docs/threat-model.md 2>/dev/null | sed -E 's/.* :: "//; s/"$//'); do grep -rqF "$t" core/src && echo "OK: $t" || echo "MISSING: $t"; done</automated>
  </verify>
  <acceptance_criteria>
    - `docs/threat-model.md` exists (file present; `docs/` directory created).
    - The doc is a THREAT-NN-keyed table: it contains the strings `THREAT-01`, `THREAT-02`, `THREAT-03`, `THREAT-04`, `THREAT-05`, `THREAT-06`, `THREAT-07`, `THREAT-08`, `THREAT-09`, `THREAT-10` (grep each present).
    - Every cited `*.test.ts :: "<it-name>"` resolves to a real test string in `packages/core/src` — the verify loop prints `OK:` for every cited name and `MISSING:` for none.
    - The THREAT-06 row contains explicit honesty-boundary wording (e.g. "emission", "NOT" real CHIPS / partition "enforcement", per D-11).
    - THREAT-01 and THREAT-10 cite the SAME hardened roundtrip `it(...)` name (the one in 04-01-SUMMARY.md).
    - THREAT-02 appears as three facets (entropy, one-time-use, TTL) each citing a distinct test.
    - `cd packages/core && pnpm test` exits 0 (the suite the doc cites is green).
    - No `docs/architecture.md` file is created.
    - No emoji in the doc body; no SPDX/license header line.
  </acceptance_criteria>
  <done>docs/threat-model.md enumerates every canonical THREAT-NN, each row citing a green test::name verified against the live suite; the partitioned-cookie row states the D-11 honesty boundary; architecture.md is not created.</done>
</task>

<task type="auto">
  <name>Task 2: Redirect CLAUDE.md's docs/architecture.md pointer to docs/threat-model.md (D-08)</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (line ~125 under "When extending the project": `- Read \`docs/architecture.md\` and \`docs/threat-model.md\` at session start.` — the ONLY line naming architecture.md; AND lines ~63-77 "Threat model discipline" which already point only at docs/threat-model.md and carry the discipline rule — leave those intact)
    - .planning/phases/04-threat-model-roundtrip-hardening/04-RESEARCH.md (lines 215-227 — the exact D-08 finding: architecture.md appears on exactly one line; the discipline rule must NOT be weakened)
    - .planning/phases/04-threat-model-roundtrip-hardening/04-PATTERNS.md (lines 175-192 — the single-line edit; do NOT touch the discipline lines)
  </read_first>
  <action>
    Edit the single CLAUDE.md line that names `docs/architecture.md` (the session-start pointer,
    ~line 125). Since `docs/architecture.md` is intentionally NOT authored this phase (D-07),
    redirect the pointer to `docs/threat-model.md` as the invariant-registry home. Suggested wording
    (exact phrasing is discretion): `- Read \`docs/threat-model.md\` at session start (the invariant
    registry).`

    Do NOT touch the "Threat model discipline" section (the rule that any change touching the
    bridge / consume routes / transferStore / cookie / wrapper-detection requires a corresponding
    threat-model.md update) — D-08 says keep it intact, do not weaken it. After the edit, `docs/
    architecture` must appear NOWHERE in CLAUDE.md.

    This is a docs/config pointer edit only — no code, no test change.
  </action>
  <verify>
    <automated>! grep -q "docs/architecture" CLAUDE.md && grep -q "docs/threat-model.md" CLAUDE.md && echo "POINTER-REDIRECTED"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "docs/architecture" CLAUDE.md` returns 0 (no architecture.md reference remains).
    - `grep -c "docs/threat-model.md" CLAUDE.md` returns >= 2 (the redirected session-start pointer PLUS the retained discipline references).
    - The "Threat model discipline" rule ("Any change touching the bridge / consume routes ... requires a corresponding update to threat-model.md") is unchanged (grep confirms the sentence is still present verbatim).
    - No `docs/architecture.md` file was created (D-07).
  </acceptance_criteria>
  <done>CLAUDE.md's single architecture.md pointer is redirected to docs/threat-model.md; the threat-model-discipline rule is intact; no architecture.md reference remains.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan produces the DOCUMENT enumerating the trust boundaries; it has no runtime trust boundary
of its own (a markdown artifact + a config pointer edit). The boundaries it documents:

| Boundary | Description |
|----------|-------------|
| popup -> opener (postMessage) | Documented as THREAT-03 (origin+source pin) |
| client -> /auth/bridge and /auth/consume | Documented as THREAT-04/05/06/09 (session gate, decoy exclusion, one-time handle, no token in body) |
| every client-constructed URL | Documented as THREAT-07/09/10 (no token in URL) |
| code -> partitioned cookie | Documented as THREAT-06 with the D-11 honesty boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Repudiation/Integrity | docs/threat-model.md citation accuracy | mitigate | D-04 enforced: every row's test::name is verified against the green suite (the Task 1 verify loop prints OK for every cited name, MISSING for none) before the plan is done — a dead citation is a HARDEN-01 defect |
| T-04-06 | Information Disclosure | THREAT-06 partitioned-cookie row | accept | The doc states the D-11 honesty boundary explicitly (emission + data flow proven, real CHIPS enforcement is a manual/browser check) rather than over-claiming browser partition isolation |
| T-04-07 | Tampering | CLAUDE.md discipline rule | mitigate | The D-08 edit is scoped to the single architecture.md pointer line; the threat-model-discipline rule is left intact (verified: the discipline sentence still present verbatim) |
| T-04-SC | Tampering | npm/pip/cargo installs | n/a | No package installs this phase — zero new dependencies |
</threat_model>

<verification>
- `docs/threat-model.md` exists and is a THREAT-NN-keyed table (THREAT-01..THREAT-10 all present).
- Every cited `*.test.ts :: "<it-name>"` resolves to a real, green test (the Task 1 verify loop prints only OK, no MISSING).
- `cd packages/core && pnpm test` exits 0 (the cited suite is green).
- THREAT-06 row states the D-11 honesty boundary; THREAT-01 and THREAT-10 dual-cite the same hardened roundtrip it() name.
- `grep -c "docs/architecture" CLAUDE.md` returns 0; the discipline rule is intact.
- `docs/architecture.md` does NOT exist (D-07).
</verification>

<success_criteria>
- HARDEN-01: docs/threat-model.md enumerates every Mode A security property and maps each invariant to a specific, currently-green test (D-03/D-04).
- D-05/D-07: THREAT-NN is the canonical namespace; the invariant registry is self-contained in threat-model.md; architecture.md is not created.
- D-08: CLAUDE.md's session-start pointer is redirected to docs/threat-model.md; the threat-model-discipline rule is intact.
- D-11: the partitioned-cookie row states the emission-not-enforcement honesty boundary explicitly.
</success_criteria>

<output>
Create `.planning/phases/04-threat-model-roundtrip-hardening/04-02-SUMMARY.md` when done.
</output>
