---
phase: 06-release-engineering
plan: 04
type: execute
wave: 3
depends_on:
  - 06-01
  - 06-02
  - 06-03
files_modified:
  - docs/release-governance.md
  - docs/recipes/authjs-cross-context-bridge.mdx
autonomous: false
requirements:
  - RELEASE-04
  - RELEASE-05
user_setup:
  - service: npm
    why: "First live publish target + name ownership for next-auth-bridge"
    env_vars:
      - name: NPM_TOKEN
        source: "npmjs.com -> Access Tokens -> Generate (automation token, publish scope) -> add as GitHub repo secret NPM_TOKEN"
    dashboard_config:
      - task: "Verify the next-auth-bridge name is owned/claimable (npm view next-auth-bridge)"
        location: "npmjs.com"
  - service: github
    why: "Branch protection on main + release-bot bypass actor for the @semantic-release/git CHANGELOG re-commit"
    dashboard_config:
      - task: "Apply branch-protection ruleset on main (PR-only, required PR-Title check) with a bypass actor for the release workflow token"
        location: "GitHub repo Settings -> Rules/Branches (or gh api)"
must_haves:
  truths:
    - "A human inspects the first tarball via npm publish --dry-run / pnpm pack before any live publish and confirms it contains only dist/ (index + store/kv, both js+d.ts) with no stray src/tests/configs"
    - "semantic-release --dry-run runs clean and derives a next version without publishing"
    - "Branch protection on main (PR-only, required PR-title check) is documented with the exact gh recipe and applied maintainer-side, with a bypass actor for the release git re-commit"
    - "An Auth.js docs recipe MDX for the cross-context bridge exists and a cross-repo PR is opened against nextauthjs/next-auth"
  artifacts:
    - path: "docs/release-governance.md"
      provides: "Branch-protection ruleset recipe + NPM_TOKEN setup + D-02 dry-run gate procedure (RELEASE-04)"
      contains: "branch protection"
    - path: "docs/recipes/authjs-cross-context-bridge.mdx"
      provides: "The recipe content authored in-repo, ready to copy into the authjs PR (RELEASE-05)"
      contains: "next-auth-bridge"
  key_links:
    - from: "docs/release-governance.md"
      to: "main branch protection ruleset"
      via: "documented gh api recipe the maintainer applies GitHub-side"
      pattern: "protection"
    - from: "docs/recipes/authjs-cross-context-bridge.mdx"
      to: "nextauthjs/next-auth docs/pages/guides/<slug>.mdx + _meta.js"
      via: "cross-repo PR copies the recipe + registers it in _meta.js"
      pattern: "guides"
---

<objective>
Land the release-safety gate and the discovery/governance docs: the D-02 human-inspected dry-run/pack
gate before the first irreversible publish, the RELEASE-04 branch-protection documentation (applied
maintainer-side) with the bypass actor the keep-git decision requires, and the RELEASE-05 Auth.js docs
recipe authored in-repo and opened as a cross-repo PR.

Purpose: This is the final, human-gated layer. npm publishes are irreversible per version, so a person
must inspect exactly what ships before the pipeline goes live (D-02). Branch protection on `main`
(RELEASE-04) is GitHub server-side state, not a repo file — this plan documents the exact recipe and
the maintainer applies it. The Auth.js recipe (RELEASE-05) is an external cross-repo PR that gives the
ecosystem a discovery path. This plan depends on Plans 01-03 (build emits a correct dist, package.json
has the publish metadata, the release config + workflows exist) so the dry-run inspects the real,
final tarball. It is `autonomous: false` — three human checkpoints (dry-run sign-off, branch-protection
apply, cross-repo PR).

Output: `docs/release-governance.md`, `docs/recipes/authjs-cross-context-bridge.mdx`, a human-signed
dry-run/pack inspection, an applied branch-protection ruleset, and an opened authjs.dev recipe PR.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-release-engineering/06-CONTEXT.md
@.planning/phases/06-release-engineering/06-RESEARCH.md
@.planning/phases/06-release-engineering/06-PATTERNS.md
@.planning/phases/06-release-engineering/06-VALIDATION.md
</context>

<artifacts_this_phase_produces>
This plan creates:
- NEW file: `docs/release-governance.md` (branch-protection gh recipe + NPM_TOKEN setup + D-02 dry-run procedure)
- NEW file: `docs/recipes/authjs-cross-context-bridge.mdx` (the recipe content, in-repo source for the external PR)
- EXTERNAL (no in-repo file): a PR against `nextauthjs/next-auth` adding `docs/pages/guides/<slug>.mdx` + a `_meta.js` line
- GitHub server-side state (no file): branch-protection ruleset on `main`; `NPM_TOKEN` repo secret

The source-grounding/drift pass MUST treat `docs/release-governance.md` and
`docs/recipes/authjs-cross-context-bridge.mdx` as newly-created artifacts (not pre-existing drift).
Branch protection and NPM_TOKEN are out-of-repo runtime state, not files.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Author docs/release-governance.md (branch-protection recipe + NPM_TOKEN + dry-run procedure)</name>
  <read_first>
    - .planning/phases/06-release-engineering/06-RESEARCH.md (Code Examples → "Branch-protection ruleset via gh api (RELEASE-04)"; Pitfall 4 → @semantic-release/git needs a bypass actor on protected main, or drop git; Runtime State Inventory → NPM_TOKEN repo secret, npm name ownership check, per-clone hook activation; Assumptions A1/A2/A4)
    - .planning/phases/06-release-engineering/06-02-PLAN.md (the recorded keep-git decision — main stays PR-only but the release workflow token gets a ruleset bypass so @semantic-release/git can push the CHANGELOG re-commit with [skip ci])
    - docs/threat-model.md (existing docs/ prose style + heading conventions to match — do not edit it, just match tone)
    - CLAUDE.md (Development workflow: main = published surface, PR-only, branch-protected; dev = engineering; commit-msg hook activation via core.hooksPath)
  </read_first>
  <action>
    Create docs/release-governance.md documenting the maintainer-applied governance steps. Sections:
    (1) NPM_TOKEN setup — create an npm automation token with publish scope at npmjs.com, add it as the
    GitHub repo secret NPM_TOKEN; verify the next-auth-bridge name is owned/claimable (`npm view
    next-auth-bridge`). (2) Branch protection on main — the exact gh recipe from RESEARCH Code Examples
    (PR-required, required status check = the PR-Title check, no direct push), AND the bypass actor for
    the release workflow token so @semantic-release/git can push the CHANGELOG re-commit to protected
    main (per the keep-git decision in Plan 02; cross-reference Pitfall 4). State explicitly that the
    bypass is scoped to the release automation, not a general admin bypass. (3) Hook activation — `git
    config core.hooksPath .githooks` is per-clone; the commit-msg + pre-commit hooks are inert until
    run; external contributors are covered by the PR-title CI check. (4) The D-02 dry-run gate procedure
    (the same steps Task 2 executes) so it is a documented, repeatable governance step. Write it as
    self-documenting maintainer-facing prose; NO internal RELEASE-NN/D-NN/THREAT-NN markers (this doc
    may ship in the repo — keep it clean); no emoji in code blocks.
  </action>
  <verify>
    <automated>test -f docs/release-governance.md && grep -qi "branch protection" docs/release-governance.md && grep -q "NPM_TOKEN" docs/release-governance.md && grep -qi "core.hooksPath" docs/release-governance.md && grep -qi "dry-run" docs/release-governance.md && test "$(grep -c 'RELEASE-\|D-0\|THREAT-' docs/release-governance.md)" = "0" && ! head -10 docs/release-governance.md | grep -Eqi 'SPDX|Copyright|@license'</automated>
  </verify>
  <acceptance_criteria>
    - docs/release-governance.md exists
    - Documents the NPM_TOKEN repo-secret setup and the npm name-ownership check
    - Documents the exact gh branch-protection recipe for main (PR-only + required PR-title check)
    - Documents the release-token bypass actor for the @semantic-release/git re-commit (cross-references the keep-git decision)
    - Documents the per-clone `core.hooksPath` hook activation
    - Documents the D-02 dry-run/pack inspection procedure
    - `grep -c 'RELEASE-\|D-0\|THREAT-' docs/release-governance.md` returns 0; no emoji in code blocks
    - The new file docs/release-governance.md carries NO per-file SPDX / `Copyright` / `@license` header (RELEASE-04 declaration-once reconciliation: license lives only in root LICENSE + package.json `"license"`, never per-file)
  </acceptance_criteria>
  <done>docs/release-governance.md gives the maintainer the complete, marker-free recipe for NPM_TOKEN, branch protection (with the release-bot bypass), hook activation, and the dry-run gate.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: D-02 dry-run / pack-inspection human gate before first live publish</name>
  <read_first>
    - .planning/phases/06-release-engineering/06-RESEARCH.md (D-02; Pitfall 3 — first tarball must contain dist/index.{js,d.ts} + dist/store/kv.{js,d.ts}, no src/tests; RELEASE-01 dry-run smoke `semantic-release --dry-run`)
    - .planning/phases/06-release-engineering/06-VALIDATION.md (Manual-Only Verifications: first live npm tarball is correct — the D-02 gate; Sampling Rate: before /gsd-verify-work, semantic-release --dry-run clean + D-02 human tarball inspection)
    - packages/core/package.json (files: ["dist"] allowlist + publishConfig from Plan 02 govern what packs)
    - docs/release-governance.md (the D-02 procedure documented in Task 1)
  </read_first>
  <what-built>
    The full release surface is now in place: tsdown build emitting dist/ (Plan 01), D-03 publish
    metadata + .releaserc.json (Plan 02), and the publish workflow + governance (Plan 03). This gate
    executes the automated dry-run/pack inspection and surfaces the tarball contents for a human to
    sign off BEFORE any live publish. The publish itself remains gated on this approval.
  </what-built>
  <action>
    Run the automated inspection first, then present results for human sign-off. (a) Build:
    `pnpm --filter next-auth-bridge build`. (b) Pack-inspect: `pnpm --filter next-auth-bridge pack
    --dry-run` (or `cd packages/core && npm publish --dry-run`) and capture the file list. (c)
    semantic-release config validation: `pnpm exec semantic-release --dry-run` from the repo root with
    a GITHUB_TOKEN available (no NPM_TOKEN needed for dry-run; it must NOT publish) and capture the
    derived next version. If GITHUB_TOKEN is ABSENT from the environment, do NOT silently skip the
    semantic-release dry-run: STOP and flag to the human that the version-derivation check (RELEASE-01
    validity) cannot run without a live GITHUB_TOKEN, and ask them to supply one or explicitly accept
    the gap. The dry-run result (the derived next version, or the missing-token flag) MUST be surfaced
    to the human in the sign-off, never swallowed. Assert from the pack output: the tarball contains dist/index.js,
    dist/index.d.ts, dist/store/kv.js, dist/store/kv.d.ts; it contains NO src/, NO __tests__/, NO
    tsdown.config.ts, NO *.test.ts; LICENSE + README are present (npm auto-includes). Present the file
    list + the derived version to the human.
  </action>
  <how-to-verify>
    1. Confirm the pack-dry-run file list contains exactly the dist artifacts (index + store/kv, js + d.ts) plus LICENSE + README + package.json, and NOTHING from src/, __tests__/, or config files.
    2. Confirm `semantic-release --dry-run` exited 0 and printed a derived next version (e.g. the first release version) with no publish attempted.
    2a. If `GITHUB_TOKEN` was absent the dry-run could not run — the executor must have surfaced that
    explicitly (not skipped it). Either supply a token and re-run, or explicitly accept the
    version-derivation gap before approving; do NOT approve on a silently-skipped dry-run.
    3. Confirm `files: ["dist"]` is honored (no stray files leaked).
    4. If anything is wrong (stray file, missing subpath, dry-run error), STOP — do not approve; fix in the relevant plan and re-run.
  </how-to-verify>
  <verify>
    <automated>pnpm --filter next-auth-bridge build && pnpm --filter next-auth-bridge pack --dry-run 2>&1 | tee /tmp/pack-list.txt && grep -q "dist/store/kv.js" /tmp/pack-list.txt && grep -q "dist/index.js" /tmp/pack-list.txt && ! grep -qE "src/|__tests__/|\.test\.ts|tsdown.config" /tmp/pack-list.txt</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter next-auth-bridge pack --dry-run` lists dist/index.js, dist/index.d.ts, dist/store/kv.js, dist/store/kv.d.ts
    - The pack list contains NO src/, NO __tests__/, NO *.test.ts, NO tsdown.config.ts
    - `pnpm exec semantic-release --dry-run` exits 0 and derives a next version without publishing
    - The semantic-release dry-run result (derived next version) is SURFACED to the human; if
      GITHUB_TOKEN is absent the executor flagged it and did NOT silently skip the RELEASE-01
      version-derivation check
    - A human has reviewed the tarball file list and the derived version and approved
  </acceptance_criteria>
  <resume-signal>Type "approved" after confirming the tarball contains only the dist surface and the dry-run derived a clean version, or describe what is wrong.</resume-signal>
  <done>The first tarball contents and the semantic-release version derivation are human-verified clean; the pipeline is cleared to go live on the next merge to main.</done>
</task>

<task type="auto">
  <name>Task 3: Author the Auth.js cross-context bridge recipe MDX (in-repo source)</name>
  <read_first>
    - .planning/phases/06-release-engineering/06-RESEARCH.md (Code Examples → "Auth.js docs recipe PR (RELEASE-05)": recipe lives at nextauthjs/next-auth docs/pages/guides/<slug>.mdx + _meta.js registration; model on sibling guides integrating-third-party-backends.mdx, creating-a-framework-integration.mdx, corporate-proxy.mdx; Assumption A3 — re-confirm the Nextra docs/pages/guides structure before opening the PR, sites occasionally migrate to Fumadocs)
    - docs/threat-model.md (the bridge's security properties — source the recipe's correctness framing from here: 256-bit one-time codes, no token in URL, PKCE preserved, partitioned cookie, postMessage origin checks)
    - CLAUDE.md (What this project is — Mode A popup/CHIPS description for the recipe narrative; no emoji in code, OK in README/MD headings)
    - packages/core/src/index.ts (the public API the recipe shows: createAuthBridge + helpers — read to cite the real exported surface, not invented symbols)
  </read_first>
  <action>
    Create docs/recipes/authjs-cross-context-bridge.mdx as the in-repo source of the recipe (so it is
    versioned here and the cross-repo PR copies it). Content: a guide-style walkthrough of the
    cross-context iframe bridge pattern for Auth.js + Next.js — the problem (Next.js app embedded in an
    enterprise iframe with an existing OIDC session), the architecture (popup runs top-level, silent
    auth, one-time-use transfer store, opaque handle exchanged for a partitioned CHIPS cookie), and a
    minimal usage example using the REAL public API (createAuthBridge wiring bridge + consume; the
    package's exports map `.` and `./store/kv`). Reference the package by its npm name next-auth-bridge.
    Keep the security framing honest and sourced from docs/threat-model.md (no token in URL, 256-bit
    one-time codes, PKCE preserved, postMessage origin checks). Use MDX/Markdown headings; NO emoji in
    code blocks; NO internal RELEASE-NN/D-NN/THREAT-NN markers (this ships publicly to authjs.dev). Pick
    a clear slug for the future PR (e.g. cross-context-iframe-bridge) and note it at the top of the file
    as a comment for the PR author. Verify the public symbols cited actually exist in
    packages/core/src/index.ts before writing them.
  </action>
  <verify>
    <automated>test -f docs/recipes/authjs-cross-context-bridge.mdx && grep -q "next-auth-bridge" docs/recipes/authjs-cross-context-bridge.mdx && grep -q "createAuthBridge" docs/recipes/authjs-cross-context-bridge.mdx && grep -q "createAuthBridge" packages/core/src/index.ts && test "$(grep -c 'RELEASE-\|D-0\|THREAT-' docs/recipes/authjs-cross-context-bridge.mdx)" = "0" && ! head -10 docs/recipes/authjs-cross-context-bridge.mdx | grep -Eqi 'SPDX|Copyright|@license'</automated>
  </verify>
  <acceptance_criteria>
    - docs/recipes/authjs-cross-context-bridge.mdx exists
    - The recipe references the npm name next-auth-bridge and the real public API symbol createAuthBridge (confirmed to exist in packages/core/src/index.ts)
    - The recipe describes the Mode A popup/CHIPS architecture and the honest security properties (no token in URL, one-time codes, PKCE, postMessage origin)
    - A target slug for the authjs PR is noted at the top of the file
    - `grep -c 'RELEASE-\|D-0\|THREAT-' docs/recipes/authjs-cross-context-bridge.mdx` returns 0; no emoji in code blocks
    - The new file docs/recipes/authjs-cross-context-bridge.mdx carries NO per-file SPDX / `Copyright` / `@license` header (RELEASE-04 declaration-once reconciliation; the slug/front-matter comment does not add a license header)
  </acceptance_criteria>
  <done>The recipe MDX exists in-repo, cites the real public API and honest security properties, and is ready to copy into the authjs cross-repo PR.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: Open the cross-repo Auth.js docs recipe PR (RELEASE-05)</name>
  <read_first>
    - docs/recipes/authjs-cross-context-bridge.mdx (the recipe source authored in Task 3 — the content to copy into the PR)
    - .planning/phases/06-release-engineering/06-RESEARCH.md (Code Examples → the exact authjs path docs/pages/guides/<slug>.mdx + the _meta.js registration line; fork → branch from main → PR against nextauthjs/next-auth; Assumption A3 re-confirm the structure before opening)
  </read_first>
  <what-built>
    The recipe content exists in-repo (Task 3). Opening the PR is an external cross-repo action against
    nextauthjs/next-auth — a forked repo, a new branch, a copied MDX file, a _meta.js line, and a PR.
    This is genuinely manual: it touches an external repository the maintainer owns the relationship
    with, and the structure must be re-confirmed live (A3) before opening.
  </what-built>
  <action>
    The maintainer (or Claude with gh access to a fork) performs: (1) re-confirm the live structure —
    `gh api repos/nextauthjs/next-auth/contents/docs/pages/guides` still shows the Nextra
    guides directory with _meta.js (A3; if it migrated to Fumadocs, adapt the path). (2) Fork
    nextauthjs/next-auth, branch from main. (3) Add docs/pages/guides/<slug>.mdx (copy the in-repo
    recipe, adapting front-matter/imports to the authjs docs conventions). (4) Add one line registering
    the slug in docs/pages/guides/_meta.js. (5) Open the PR against nextauthjs/next-auth and record the
    PR URL in the plan summary. No internal markers or emoji in the PR content.
  </action>
  <how-to-verify>
    1. Confirm the authjs guides directory structure (Nextra docs/pages/guides + _meta.js) is current.
    2. Confirm the PR adds exactly one <slug>.mdx file under docs/pages/guides/ and one _meta.js line.
    3. Confirm the PR is open against nextauthjs/next-auth and the URL is recorded.
  </how-to-verify>
  <resume-signal>Paste the opened PR URL (against nextauthjs/next-auth), or note if the docs structure migrated and the path was adapted.</resume-signal>
  <done>A recipe PR is open against nextauthjs/next-auth adding the cross-context bridge guide + its _meta.js registration, with the PR URL recorded.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 5: Apply branch protection on main (RELEASE-04, maintainer GitHub-side)</name>
  <read_first>
    - docs/release-governance.md (the gh branch-protection recipe + the release-token bypass actor authored in Task 1)
    - .planning/phases/06-release-engineering/06-RESEARCH.md (Code Examples → gh api branch-protection recipe; Pitfall 4 → bypass actor for @semantic-release/git; Assumption A4 → classic protection vs rulesets API differ, maintainer adjusts)
  </read_first>
  <what-built>
    The branch-protection recipe is documented (Task 1). Applying it is GitHub server-side state, not a
    repo file — only the maintainer (or gh with admin scope) can set it. This gate applies it and
    verifies main is PR-only with the required PR-title check and the release-bot bypass.
  </what-built>
  <action>
    The maintainer applies branch protection on main per docs/release-governance.md: require PRs, require
    the PR-Title status check, block direct pushes, and add the bypass actor for the release workflow
    token so @semantic-release/git can push the CHANGELOG re-commit (keep-git decision). Use either the
    gh api recipe or the GitHub web UI (A4 — the two API shapes differ; adjust as needed). Verify a
    direct push to main is rejected and the ruleset shows the required check + the scoped bypass.
  </action>
  <how-to-verify>
    1. Attempt a direct push to main (or a dry equivalent) and confirm it is rejected for non-bypass actors.
    2. Confirm the ruleset requires a PR and lists the PR-Title check as a required status check.
    3. Confirm the release automation token is the only bypass actor (not a blanket admin bypass).
  </how-to-verify>
  <resume-signal>Type "applied" after confirming main blocks direct pushes, requires the PR-title check, and allows only the release-bot bypass — or describe what differs.</resume-signal>
  <done>main has branch protection blocking direct pushes, requires the PR-title check, and permits only the scoped release-automation bypass for the changelog re-commit (RELEASE-04).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local repo → npm registry (first publish) | the D-02 gate is the last human line before an irreversible publish |
| any actor → main (published surface) | branch protection governs what reaches the release-parsing surface |
| this repo → external authjs.dev repo | the recipe PR crosses into a repo the maintainer does not control |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-13 | Information Disclosure | first published tarball contents | mitigate | D-02 human gate (Task 2): `pnpm pack --dry-run` file list is inspected to confirm only dist/ ships (no src/tests/configs/secrets) before the irreversible publish; `files: ["dist"]` from Plan 02 enforces it |
| T-06-14 | Tampering | unreviewed direct push to main | mitigate | branch protection (Task 5): main is PR-only with the required PR-title check; only the scoped release-automation token bypasses, for the [skip ci] CHANGELOG re-commit |
| T-06-15 | Elevation | over-broad branch-protection bypass | mitigate | the bypass actor is scoped to the release workflow token, NOT a general admin bypass (documented in Task 1, verified in Task 5) — Pitfall 4 is resolved without weakening protection for humans |
| T-06-16 | Spoofing | recipe PR misrepresents the package's guarantees | accept | the recipe's security claims are sourced verbatim from docs/threat-model.md (honest framing); the cross-repo PR is human-authored/reviewed; low risk — it is documentation, not executable supply chain |

No change in this plan touches the bridge / consume / cookie / wrapper-detection logic; it is docs +
governance + a human publish gate only. No threat-model.md invariant is modified. No threat-model.md
update is required.
</threat_model>

<verification>
- docs/release-governance.md documents NPM_TOKEN, branch protection (with bypass), hook activation, and the dry-run gate.
- The D-02 gate confirms the tarball contains only dist/ (index + store/kv) and semantic-release --dry-run derives a clean version.
- docs/recipes/authjs-cross-context-bridge.mdx cites the real public API and honest security properties.
- A recipe PR is open against nextauthjs/next-auth (URL recorded).
- main has branch protection applied (direct push rejected, PR-title required, scoped bypass).
- No internal markers or emoji in any committed file this plan touches.
</verification>

<success_criteria>
- The first tarball is human-inspected clean before any live publish (D-02).
- main has branch protection blocking direct pushes (RELEASE-04, license half already satisfied by root LICENSE + package.json).
- An Auth.js docs recipe PR is opened against authjs.dev (RELEASE-05).
</success_criteria>

<output>
Create `.planning/phases/06-release-engineering/06-04-SUMMARY.md` when done.
</output>
