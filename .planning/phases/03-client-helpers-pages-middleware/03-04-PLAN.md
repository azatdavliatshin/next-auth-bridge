---
phase: 03-client-helpers-pages-middleware
plan: 04
type: execute
wave: 3
depends_on: ["03-01", "03-02", "03-03"]
files_modified:
  - packages/core/src/index.ts
  - packages/core/src/__tests__/roundtrip.e2e.test.ts
autonomous: true
requirements: [CLIENT-05]
must_haves:
  truths:
    - "The public surface (index.ts) re-exports detectContext, isTrustedMessage, runPopupFlow, openAuthPopup, createBridgeMiddleware, and the BridgeContext type"
    - "An end-to-end pure-Node test drives the REAL bridge and consume handlers with plain Request objects: bridge mints a 200 { code }, a function-level postMessage simulation passes the code to the opener, the opener drives consume which 302s with per-chunk Partitioned Set-Cookie"
    - "The E2E asserts the session-token value never appears in any client-constructed URL across the flow; the opaque code MAY appear in the consume URL (D-15)"
    - "The Partitioned attribute is emitted on the consume Set-Cookie(s); the test comment states the honesty boundary (emission only, not real partition enforcement — Phase 4 browser check)"
    - "pnpm build re-emits dist with the new exports and the ./store/kv subpath still resolves"
  artifacts:
    - path: "packages/core/src/index.ts"
      provides: "extended public surface — the four Phase 3 functions + BridgeContext type"
      contains: "export { createBridgeMiddleware }"
    - path: "packages/core/src/__tests__/roundtrip.e2e.test.ts"
      provides: "the headline first end-to-end iframe→bridge→consume→Partitioned-cookie flow on the pure-Node bench + the THREAT-07 token-vs-handle URL-hygiene assertion"
  key_links:
    - from: "packages/core/src/__tests__/roundtrip.e2e.test.ts"
      to: "createBridgeHandler / createConsumeHandler (Phase 2, real handlers)"
      via: "driven with plain Request objects + in-memory store"
      pattern: "bridge|consume"
    - from: "packages/core/src/__tests__/roundtrip.e2e.test.ts"
      to: "the Partitioned Set-Cookie emission"
      via: "getSetCookie() assertion"
      pattern: "Partitioned"
    - from: "packages/core/src/index.ts"
      to: "the four Phase 3 modules"
      via: "additive re-exports (middleware stays in the main entry as a separate symbol, NOT a subpath — D-16)"
      pattern: "export \\{"
---

<objective>
Wire the Phase 3 public surface (`index.ts` re-exports the four new functions + the `BridgeContext` type) and write the headline end-to-end test (D-11): the first complete iframe→bridge→consume→partitioned-cookie flow on the existing pure-Node Vitest bench, plus the THREAT-07 client-side URL-hygiene assertion (CLIENT-05). This is Wave 3 — it depends on every source symbol from Waves 1-2 and drives the REAL Phase 2 handlers.

Purpose: This is success criterion 5 — "the first end-to-end flow runs green on the Vitest test bench." It drives the real `bridge`/`consume` handlers with plain `Request` objects, models the popup↔opener `postMessage` handoff as a function-level simulation (pass the `{ code }` from the bridge response into the consume call), and asserts the full data flow AND the `Partitioned` `Set-Cookie` emission end-to-end. CLIENT-05/THREAT-07 closes the client-side URL-hygiene property at this seam: the session TOKEN never appears in any client-constructed URL, while the opaque one-time handle MAY appear in the consume URL (D-15 — do NOT mis-specify this).

Output: extended `index.ts`; `roundtrip.e2e.test.ts`; a green full suite; a re-emitted `dist` proving the new exports resolve and the `./store/kv` subpath is unaffected.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-client-helpers-pages-middleware/03-CONTEXT.md
@.planning/phases/03-client-helpers-pages-middleware/03-RESEARCH.md
@.planning/phases/03-client-helpers-pages-middleware/03-PATTERNS.md
@.planning/phases/03-client-helpers-pages-middleware/03-VALIDATION.md
@CLAUDE.md
@packages/core/src/bridge-route.ts
@packages/core/src/consume-route.ts
</context>

<artifacts_this_phase_produces>
This plan (03-04) produces:
- **Modified:** `packages/core/src/index.ts` (additive re-exports — `detectContext`, `isTrustedMessage`, `runPopupFlow`, `openAuthPopup`, `createBridgeMiddleware`, and `export type { BridgeContext }`). The middleware stays in the MAIN entry as a separate symbol — NOT a new subpath export (its Edge-safety is enforced by its own store-free import graph per D-16, not by a subpath boundary). NO `.tsx`/React entry (D-13).
- **New file:** `packages/core/src/__tests__/roundtrip.e2e.test.ts` (the headline D-11 E2E + the THREAT-07 token-vs-handle URL-hygiene assertion)
- **Build artifact:** `dist/index.{js,d.ts}` re-emitted with the new exports (standard re-build, not a state migration)

This plan does NOT create new runtime symbols — it wires and proves the symbols delivered by 03-01/02/03.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: extend the public surface (index.ts) + re-emit dist</name>
  <files>packages/core/src/index.ts</files>
  <read_first>
    - packages/core/src/index.ts — the existing additive-export style: grouped, commented `export` / `export type` blocks (lines 8-31); `auth-helpers` exported as bare functions (lines 25-26); KV stays subpath-only (03-PATTERNS.md §index.ts)
    - .planning/phases/03-client-helpers-pages-middleware/03-PATTERNS.md §"src/index.ts (public surface — additive)" — the exact additive export list to add
    - .planning/phases/03-client-helpers-pages-middleware/03-RESEARCH.md §"Runtime State Inventory" — the middleware stays in the MAIN entry as a separate symbol (NOT a subpath); re-run `pnpm build` after adding exports
    - .planning/phases/03-client-helpers-pages-middleware/03-CONTEXT.md D-13 (no .tsx/React in the package), D-16 (middleware is a separate export but in the main entry; isolation is its import graph, not a subpath)
  </read_first>
  <action>
    Extend `index.ts` additively (mirror the existing grouped/commented export style). Add: `export { detectContext } from "./detect-context.js";`, `export { isTrustedMessage } from "./is-trusted-message.js";`, `export { runPopupFlow } from "./popup-flow.js";`, `export { openAuthPopup } from "./open-auth-popup.js";`, `export { createBridgeMiddleware } from "./middleware.js";`, and `export type { BridgeContext } from "./detect-context.js";`. Keep these as bare re-exports (not bundled into any factory return — same discipline as the `getAuthCookieName`/`sanitizeNext` exports). Add a short grouping comment for the Phase 3 client surface; the comment may note that the middleware stays in the main entry as a separate symbol whose Edge-safety comes from its own store-free import graph. Do NOT add a `./react` or any `.tsx` subpath (D-13). Do NOT re-export the KV adapter (it stays behind `./store/kv`). After editing, run `pnpm build` to re-emit `dist` and confirm the new exports compile and the `./store/kv` subpath still resolves.
  </action>
  <verify>
    <automated>cd packages/core && pnpm build && node -e "const m = require('./dist/index.js'); for (const n of ['detectContext','isTrustedMessage','runPopupFlow','openAuthPopup','createBridgeMiddleware']) { if (typeof m[n] !== 'function') { console.error('MISSING export', n); process.exit(1); } } console.log('all exports present');"</automated>
  </verify>
  <acceptance_criteria>
    - `packages/core/src/index.ts` re-exports `detectContext`, `isTrustedMessage`, `runPopupFlow`, `openAuthPopup`, `createBridgeMiddleware`, and the type `BridgeContext`
    - `pnpm build` exits 0 and re-emits `dist/index.{js,d.ts}`
    - `require('./dist/index.js')` exposes all five functions (verified by the node one-liner)
    - `grep -nE "\.tsx|/react|react" packages/core/src/index.ts` returns nothing (no React/.tsx export — D-13)
    - The `./store/kv` subpath is NOT added to the main entry (KV stays subpath-only)
  </acceptance_criteria>
  <done>index.ts exposes the four Phase 3 functions + the BridgeContext type as bare re-exports, dist re-emits cleanly with all exports resolvable, and no React/.tsx or KV is added to the main entry.</done>
</task>

<task type="auto">
  <name>Task 2: end-to-end roundtrip + THREAT-07 URL hygiene (D-11 / CLIENT-05, success criterion 5)</name>
  <files>packages/core/src/__tests__/roundtrip.e2e.test.ts</files>
  <read_first>
    - packages/core/src/__tests__/consume-route.test.ts lines 1-29 (THREAT-NN-tagged test header) and lines 330-382 (the "round-trips chunks end-to-end" test — copy this structure almost verbatim, inserting the function-level postMessage simulation between bridge and consume) (03-PATTERNS.md §roundtrip.e2e.test.ts)
    - packages/core/src/bridge-route.ts — the real bridge handler: `200 { code }` JSON, zero cookies (drive it with a plain Request via makeRequest)
    - packages/core/src/consume-route.ts — the real consume handler: `?code=&next=` → 302 + per-chunk Partitioned Set-Cookie (drive it with a plain Request; read Set-Cookie via getSetCookie() ONLY — never .get('Set-Cookie'), Pitfall 1)
    - packages/core/src/__tests__/helpers.ts — makeRequest, fakeVerifySession, makeTestStore (in-memory store, no KV on the bench)
    - .planning/phases/03-client-helpers-pages-middleware/03-CONTEXT.md D-01 (opener drives consume), D-11 + amend (pure-Node bench, function-level postMessage sim, asserts data flow + Partitioned EMISSION only — NOT real partition enforcement), D-14 (GET-fetch consume transport — locked by research, no Phase 2 change), D-15 (token-vs-handle: assert the session TOKEN never appears in a client-constructed URL; do NOT forbid ?code=)
    - .planning/phases/03-client-helpers-pages-middleware/03-RESEARCH.md §"The Decisive Item: D-14" and §"Validation Architecture → Test Map" (the success-criterion-5 row and the CLIENT-05/THREAT-07 row)
  </read_first>
  <behavior>
    - Test (E2E happy path, success criterion 5): a verified session + chunked session-token cookies → real bridge returns `200 { code }` (64-hex); a function-level postMessage simulation carries that `code` to the opener; the opener constructs the consume URL `/auth/consume?code=<code>&next=<path>` and drives the real consume handler → `302` to the sanitized `next` with one `Partitioned` Set-Cookie per stored chunk whose name=value round-trips the harvested chunks exactly
    - Test (THREAT-07 token hygiene, CLIENT-05 / D-15): across the flow, assert the session-TOKEN value (the chunk values harvested into the store, e.g. `base.tok.en`/`chunk-zero`) NEVER appears in any URL the client constructs (neither the bridge URL nor the consume URL). The opaque `code` MAY appear in the consume URL — assert it IS present there (proving the test does NOT mis-forbid `?code=`), and assert the token values are NOT present in either URL
    - Test (Partitioned emission, D-11 amend): every consume Set-Cookie contains `Partitioned`; a comment in the test states the honesty boundary — this asserts attribute EMISSION + data flow only, NOT real CHIPS partition enforcement (a Phase 4 manual/browser check)
  </behavior>
  <action>
    Create `roundtrip.e2e.test.ts`. Copy the structure of consume-route.test.ts's "round-trips chunks end-to-end" test (lines 330-382). Wire one shared in-memory store + one `AuthBridgeOptions` and build both real handlers (use `createAuthBridge` or `createBridgeHandler`/`createConsumeHandler` directly — your discretion; both drive the same store). Step 1: drive the real bridge with a `makeRequest` carrying a verified session (fakeVerifySession truthy) + chunked session-token cookies → assert `200`, read `{ code }`, assert it matches `/^[0-9a-f]{64}$/`. Step 2 (function-level postMessage simulation, D-11): instead of a real cross-window channel, pass the `{ code }` from the bridge JSON directly to the "opener" side of the test — model exactly the data the popup would post (`{ source: 'next-auth-bridge', type: 'auth-success', code }`) and have the opener extract `code` from it (optionally route it through `isTrustedMessage` with a matching origin+source to exercise the predicate in the flow). Step 3: the opener constructs the consume URL string `${ORIGIN}/auth/consume?code=${encodeURIComponent(code)}&next=${encodeURIComponent(next)}` (GET-fetch transport, D-14) and drives the real consume handler with a `makeRequest` for that URL → assert `302`, `Location` is the sanitized `next`, and `getSetCookie()` (ARRAY — never `.get`) returns one `Partitioned` cookie per chunk whose name=value matches the harvested chunks. Step 4 (THREAT-07 / D-15): collect every URL the client constructed in the flow (the bridge path and the consume URL) and assert NONE contains any session-token chunk value; assert the consume URL DOES contain the `code` (so the test correctly permits the handle in the URL but forbids the token). Header comment tags THREAT-07 and states the D-11 honesty boundary (emission asserted; real partition enforcement is a Phase 4 browser check). TEST comments MAY cite THREAT-NN; this is a test file. Read Set-Cookie via `getSetCookie()` only.
  </action>
  <verify>
    <automated>cd packages/core && pnpm test -- roundtrip</automated>
  </verify>
  <acceptance_criteria>
    - `packages/core/src/__tests__/roundtrip.e2e.test.ts` exists and `pnpm test -- roundtrip` exits 0
    - The E2E drives the REAL bridge and consume handlers (not stubs) with plain `Request` objects and the in-memory store
    - The happy path asserts `200 { code }` → 302 + per-chunk `Partitioned` Set-Cookie with round-tripped name=value (success criterion 5)
    - The THREAT-07 assertion proves the session-TOKEN value never appears in any client-constructed URL AND explicitly permits the `code` in the consume URL (D-15) — i.e. it asserts `code` IS in the consume URL and token values are NOT
    - Set-Cookie is read via `getSetCookie()` only (no `.get("Set-Cookie")` in the file)
    - A comment states the D-11 honesty boundary (emission, not real partition enforcement)
  </acceptance_criteria>
  <done>The headline pure-Node E2E drives the real bridge→(simulated postMessage)→consume flow to a 302 with per-chunk Partitioned Set-Cookie, asserts the session token never appears in a client-constructed URL while permitting the opaque code in the consume URL (D-15), and documents the emission-only honesty boundary (D-11).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| popup → opener (simulated `postMessage`) | The `{ code }` handle crosses the (function-level simulated) channel; the opener-side extraction models the validated receipt |
| opener → /auth/consume (client-constructed URL) | The consume URL is constructed by the client; the session token must never be placed in it (the opaque code may) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-10 (THREAT-07) | Information Disclosure | client-constructed consume URL | mitigate | Assert the session-token value never appears in any URL the client constructs across the flow; the token rides only in the consume `Set-Cookie`. The opaque one-time `code` in `?code=` is permitted (D-15) and explicitly asserted present, so the test does not mis-forbid it. |
| T-03-11 | Tampering | consume Set-Cookie attributes | mitigate | The E2E asserts every consume Set-Cookie carries `Partitioned` (emission). Honesty boundary documented: real partition enforcement is a Phase 4 browser check, not asserted here (D-11 amend). |
| T-03-12 | Spoofing | wrong-partition cookie (popup drives consume) | mitigate | The flow models the OPENER (not the popup) driving consume (D-01), so the partition key would be the host site; the bench asserts the data flow that places the request in the opener context. |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase (03-RESEARCH.md Package Legitimacy Audit). Test + re-export wiring only. No slopcheck checkpoint required. |
</threat_model>

<verification>
- `pnpm test -- roundtrip` exits 0 (success criterion 5 + CLIENT-05/THREAT-07)
- `pnpm test` (full suite) exits 0 — Phase 1/2/3 all green
- `pnpm build` exits 0 and `dist/index.js` exposes all five Phase 3 functions; the `./store/kv` subpath still resolves
- The E2E reads Set-Cookie via `getSetCookie()` only and asserts `Partitioned` emission with the documented honesty boundary
- THREAT-07: the session token is proven absent from every client-constructed URL; the opaque `code` is proven present in the consume URL (D-15)
</verification>

<success_criteria>
- The public surface re-exports the four Phase 3 functions + `BridgeContext`, and `dist` re-emits with them resolvable (no React/.tsx, no new subpath)
- The first end-to-end iframe→bridge→consume→partitioned-cookie flow runs GREEN on the pure-Node bench (success criterion 5 / D-11)
- THREAT-07 client-side URL hygiene is closed at this seam with the correct token-vs-handle distinction (CLIENT-05 / D-15)
- The full suite (Phase 1/2/3) is green
</success_criteria>

<output>
Create `.planning/phases/03-client-helpers-pages-middleware/03-04-SUMMARY.md` when done
</output>
