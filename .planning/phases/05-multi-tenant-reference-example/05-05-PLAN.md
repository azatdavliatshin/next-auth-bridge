---
phase: 05-multi-tenant-reference-example
plan: 05
type: execute
wave: 4
depends_on: ["05-02", "05-03", "05-04"]
files_modified:
  - examples/tenant-app/docs/live-validation.md
  - docs/threat-model.md
autonomous: false
requirements: [EXAMPLE-01, EXAMPLE-02]
user_setup:
  - service: microsoft-entra-id
    why: "A real multi-tenant Entra app registration is required for the live preview (EXAMPLE-02 has no fallback)"
    dashboard_config:
      - task: "Register a multi-tenant app; set redirect URI https://<app-origin>/api/auth/callback/microsoft-entra-id; create a client secret"
        location: "Entra portal -> App registrations"
  - service: upstash-redis
    why: "Serverless transfer store — in-memory fails by construction on Vercel"
    dashboard_config:
      - task: "Provision Upstash Redis (Vercel Marketplace -> Upstash) and link it to the tenant-app Vercel project"
        location: "Vercel dashboard -> Storage / Marketplace -> Upstash"
  - service: vercel
    why: "Two separate deploy targets (tenant app + host-shell) on distinct *.vercel.app origins (cross-site)"
    env_vars:
      - name: AUTH_SECRET
        source: "openssl rand -base64 33 (set in the Vercel tenant-app project env)"
      - name: KV_REST_API_URL
        source: "Upstash integration auto-injects (or copy from Upstash console)"
      - name: KV_REST_API_TOKEN
        source: "Upstash integration auto-injects (or copy from Upstash console)"
      - name: APP_ORIGIN
        source: "the tenant-app Vercel preview origin, e.g. https://<app>.vercel.app"
      - name: HOST_SHELL_ORIGIN
        source: "the host-shell Vercel preview origin, e.g. https://<host>.vercel.app (MUST differ from APP_ORIGIN)"

must_haves:
  truths:
    - "The tenant app and the host-shell are deployed to two DISTINCT *.vercel.app origins (genuinely cross-site)"
    - "The popup roundtrip signs the iframe in across at least two tenants on the live preview against a real Entra registration (KV-backed)"
    - "The session cookie carries Partitioned and is isolated to the host-shell top-level partition (real CHIPS enforcement observed in DevTools)"
    - "The D-10 consume transport (fetch vs navigation) is chosen from the live observation and recorded"
    - "docs/threat-model.md records the live CHIPS observation as the THREAT-06 manual-check evidence the honesty boundary points to"
  artifacts:
    - path: "examples/tenant-app/docs/live-validation.md"
      provides: "The written manual-validation procedure + the recorded observations (cross-site sign-in, CHIPS partition isolation, transport choice)"
      contains: "Partitioned"
    - path: "docs/threat-model.md"
      provides: "Updated THREAT-06 honesty boundary citing the recorded live CHIPS evidence + the resolved consume transport"
      contains: "THREAT-06"
  key_links:
    - from: "docs/threat-model.md"
      to: "examples/tenant-app/docs/live-validation.md"
      via: "THREAT-06 honesty-boundary cites the recorded live evidence"
      pattern: "live-validation|THREAT-06"
---

<objective>
Deploy the two-origin reference example live and perform the manual observations the Node bench cannot make: the cross-site popup roundtrip signing the iframe in across at least two tenants against a real Entra registration (KV-backed), the real CHIPS partition isolation of the session cookie (DevTools), and the D-10 consume-transport decision (fetch vs navigation, chosen from the observed behavior). Record the evidence and close the THREAT-06 honesty boundary in docs/threat-model.md.

Purpose: This is the v0.1.0 release gate — the live, cross-site proof that Mode A actually works and that THREAT-06's CHIPS partition enforcement holds in a real browser (the observation Phase 4 deferred to here). EXAMPLE-02 cannot be CI-automated; it requires a real multi-tenant Entra registration + a provisioned Upstash store + two Vercel deploy targets.
Output: A recorded live-validation procedure with observations, the resolved consume transport (and the seam locked to it), and the updated threat-model honesty boundary.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-multi-tenant-reference-example/05-CONTEXT.md
@.planning/phases/05-multi-tenant-reference-example/05-RESEARCH.md
@.planning/phases/05-multi-tenant-reference-example/05-VALIDATION.md
@.planning/phases/05-multi-tenant-reference-example/05-01-SUMMARY.md
@.planning/phases/05-multi-tenant-reference-example/05-02-SUMMARY.md
@.planning/phases/05-multi-tenant-reference-example/05-03-SUMMARY.md
@.planning/phases/05-multi-tenant-reference-example/05-04-SUMMARY.md
@CLAUDE.md
@docs/threat-model.md
@examples/tenant-app/.env.example
@examples/tenant-app/lib/consume-transport.ts
</context>

<artifacts_this_phase_produces>
This plan creates one NEW file and modifies one existing doc:
- `examples/tenant-app/docs/live-validation.md` — NEW: the manual validation procedure + recorded observations
- `docs/threat-model.md` — MODIFIED: the THREAT-06 honesty boundary updated with the live evidence + resolved transport
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author the live-validation procedure doc + final pre-deploy bench gate</name>
  <files>examples/tenant-app/docs/live-validation.md</files>
  <read_first>
    - .planning/phases/05-multi-tenant-reference-example/05-VALIDATION.md (the Manual-Only Verifications table: cross-site sign-in across >=2 tenants, THREAT-06 CHIPS partition isolation, D-10 transport observation)
    - .planning/phases/05-multi-tenant-reference-example/05-RESEARCH.md (the DevTools procedure: Application -> Cookies, confirm Partitioned + partition keyed to the host-shell; A6 framing; Pitfall 6 cookie attributes)
    - examples/tenant-app/.env.example (the env contract the deploy must satisfy)
    - examples/tenant-app/lib/consume-transport.ts (the swappable seam whose transport this validation will lock)
  </read_first>
  <action>
    Create `examples/tenant-app/docs/live-validation.md` — a written, step-by-step manual procedure (committed, demo-on-its-own-terms, no internal req-IDs except a legitimate reference to docs/threat-model.md as published docs) covering: (1) the two-origin deploy prerequisites (distinct tenant-app and host-shell Vercel origins; Entra redirect URI; Upstash linked; env vars set per .env.example); (2) the cross-site sign-in walkthrough — open the host-shell origin, which cross-site-iframes the tenant app at /t/acme; trigger popup sign-in; confirm the iframe shows signed-in for /t/acme AND a second tenant; (3) the DevTools CHIPS check — Application -> Cookies: confirm the session cookie carries Partitioned and appears under the partition keyed to the host-shell top-level site, and that loading the tenant app under a DIFFERENT host partition does NOT see the cookie; (4) the D-10 transport observation — with the fetch variant active, confirm the Partitioned Set-Cookie from the consume 302 commits to the iframe partition (a subsequent request carries the cookie); if it does, lock fetch; if not, switch the seam to navigation and re-observe; record the chosen transport and the evidence. Leave clearly-marked blanks for the recorded results (filled in Task 3 after the checkpoint). Before deploying, run the full bench gate to confirm nothing regressed.
  </action>
  <verify>
    <automated>cd /Users/azatdavliatshin/code/next-auth-bridge && pnpm -r test 2>&1 | grep -qE 'failed|FAIL' && { echo 'workspace tests red'; exit 1; }; node -e "const fs=require('fs'); const d=fs.readFileSync('examples/tenant-app/docs/live-validation.md','utf8'); for(const k of ['Partitioned','partition','cross-site','transport']){ if(!new RegExp(k,'i').test(d)){console.error('procedure missing: '+k);process.exit(1)} } if(/D-[0-9]|EXAMPLE-[0-9]|CLIENT-[0-9]|HARDEN-[0-9]/.test(d)){console.error('internal req-ID leaked (THREAT-NN doc-ref allowed)');process.exit(1)} console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `examples/tenant-app/docs/live-validation.md` documents the two-origin deploy prereqs, the cross-site sign-in walkthrough across >=2 tenants, the DevTools CHIPS partition-isolation check, and the D-10 transport observation — with clearly-marked blanks for recorded results
    - `pnpm -r test` exits 0 (full workspace green before deploy — packages/core + the example suites)
    - The doc contains no internal req-IDs other than a legitimate citation of docs/threat-model.md (THREAT-NN as published-doc reference is permitted)
  </acceptance_criteria>
  <done>The live-validation procedure is committed with the cross-site/CHIPS/transport steps and result blanks; the full workspace test bench is green before the live deploy.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live cross-site deploy + CHIPS/transport observation (human checkpoint)</name>
  <files>(no committed files — human provisioning + live observation only; results recorded in Task 3)</files>
  <action>
    HUMAN-ONLY checkpoint (no CLI/API substitute for a real Entra registration, a provisioned Upstash store, and two distinct Vercel deploy origins). The human provisions Entra + Upstash + Vercel env, deploys the tenant app and the host-shell to two distinct *.vercel.app origins, performs the cross-site popup sign-in across >=2 tenants, observes the CHIPS partition isolation in DevTools, and runs the D-10 transport observation (fetch first; switch the seam to navigation and re-observe if fetch does not commit the partitioned cookie). The human reports the chosen transport + the CHIPS result; Task 3 records the evidence. See the structured fields below.
  </action>
  <what-built>
    The complete two-origin reference example: the tenant app (Auth.js v5 multi-tenant Entra, /auth/bridge + /auth/consume backed by the KV store, the /auth/popup page, >=2 /t/[tenant] pages, the per-tenant manifest, the inert /install-pwa, the swappable consume seam) and the SEPARATE host-shell app that cross-site-iframes it. CI proves provider-agnosticism against Keycloak. The live-validation procedure doc is committed with result blanks. What remains is the human-only live observation: a real multi-tenant Entra registration + a provisioned Upstash store + two distinct Vercel deploy targets cannot be CI-automated (in-memory fails by construction on serverless — the KV path is what is being proven).
  </what-built>
  <how-to-verify>
    1. PROVISION (human-only — no CLI/API substitute): register a multi-tenant Entra app (redirect URI `https://<app-origin>/api/auth/callback/microsoft-entra-id`, client secret); provision Upstash Redis and link it to the tenant-app Vercel project; set the env vars per `.env.example` (AUTH_SECRET, AUTH_MICROSOFT_ENTRA_ID_*, KV_REST_API_*, APP_ORIGIN, HOST_SHELL_ORIGIN, NEXT_PUBLIC_APP_ORIGIN).
    2. DEPLOY two distinct Vercel projects from this repo — one rooted at `examples/tenant-app` (-> `<app>.vercel.app`), one at `examples/host-shell` (-> `<host>.vercel.app`). Confirm `HOST_SHELL_ORIGIN` != `APP_ORIGIN` (genuinely cross-site via the Public Suffix List).
    3. CROSS-SITE SIGN-IN: open `https://<host>.vercel.app`; it cross-site-iframes `<app>.vercel.app/t/acme`; trigger the popup sign-in; sign in with a real Entra account; confirm the iframe shows signed-in for `/t/acme` AND for a second tenant (e.g. `/t/globex`).
    4. CHIPS CHECK (DevTools -> Application -> Cookies): confirm the session cookie carries `Partitioned` and appears under the partition keyed to `<host>` (the top-level site); confirm loading the tenant app under a DIFFERENT host partition does NOT see the cookie. Capture DevTools evidence.
    5. TRANSPORT OBSERVATION (D-10): with the fetch variant active, confirm the `Partitioned` Set-Cookie from the consume 302 commits to the iframe partition (the next request carries the cookie). If it does -> lock fetch. If NOT -> switch `lib/consume-transport.ts` to the navigation variant, redeploy, and re-observe. Record which transport was chosen and the evidence.
    6. Report back: the chosen transport, the CHIPS isolation result, and confirmation the roundtrip works live across >=2 tenants. Then approve so Task 3 records the evidence and closes the threat-model honesty boundary.
  </how-to-verify>
  <verify>
    <human-check>The human confirms: two distinct origins deployed; cross-site sign-in works across >=2 tenants live against real Entra (KV-backed); the session cookie is Partitioned and isolated to the host-shell partition in DevTools; the consume transport (fetch|navigation) is chosen from the observation.</human-check>
  </verify>
  <resume-signal>Type "approved" with the chosen transport (fetch|navigation) + the CHIPS observation result, or describe what failed.</resume-signal>
  <done>The human has deployed two distinct origins, observed the live cross-site roundtrip across >=2 tenants against real Entra (KV-backed), confirmed CHIPS partition isolation in DevTools, and chosen the consume transport from the observation.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Record the observations + close the THREAT-06 honesty boundary</name>
  <files>examples/tenant-app/docs/live-validation.md, examples/tenant-app/lib/consume-transport.ts, docs/threat-model.md</files>
  <read_first>
    - docs/threat-model.md (the existing THREAT-06 row + the "Honesty boundary — THREAT-06 partitioned cookie" section that says real CHIPS enforcement is a manual check planned for Phase 5, and the fetch-vs-navigation empirical question deferred to Phase 5)
    - examples/tenant-app/docs/live-validation.md (the procedure with result blanks — from Task 1)
    - examples/tenant-app/lib/consume-transport.ts (the seam to lock to the observed transport)
    - CLAUDE.md (threat-model discipline: a change touching consume/cookie behavior requires a threat-model.md update + the relevant negative case — already covered by the Plan 04 replay test)
  </read_first>
  <action>
    Fill in `examples/tenant-app/docs/live-validation.md` with the recorded observations from the checkpoint: the cross-site sign-in result across >=2 tenants, the DevTools CHIPS partition-isolation evidence, and the chosen consume transport (fetch or navigation) with its evidence. If the checkpoint selected navigation, update `examples/tenant-app/lib/consume-transport.ts` to lock the navigation variant (otherwise confirm fetch stays active) and note the seam is now resolved. Update `docs/threat-model.md`: in the THREAT-06 honesty-boundary section, record that the live CHIPS partition-enforcement observation has now been performed (cite examples/tenant-app/docs/live-validation.md as the recorded evidence) and that the consume-transport empirical question is resolved to the chosen transport. Keep docs/threat-model.md's THREAT-NN registry intact; do not weaken any existing invariant. This satisfies the CLAUDE.md threat-model discipline (the consume-transport decision is recorded; the negative case — handle replay -> 4xx — is already covered by the Plan 04 Keycloak roundtrip test and the existing package tests).
  </action>
  <verify>
    <automated>cd /Users/azatdavliatshin/code/next-auth-bridge && node -e "const fs=require('fs'); const tm=fs.readFileSync('docs/threat-model.md','utf8'); if(!/THREAT-06/.test(tm)){console.error('THREAT-06 missing');process.exit(1)}; if(!/live-validation|live observ|observed/i.test(tm)){console.error('honesty boundary not updated with live evidence');process.exit(1)}; const lv=fs.readFileSync('examples/tenant-app/docs/live-validation.md','utf8'); if(!/(fetch|navigation)/i.test(lv)){console.error('chosen transport not recorded');process.exit(1)}; const seam=fs.readFileSync('examples/tenant-app/lib/consume-transport.ts','utf8'); if(/D-[0-9]|EXAMPLE-[0-9]|CLIENT-[0-9]|HARDEN-[0-9]/.test(seam)){console.error('internal req-ID leaked in seam');process.exit(1)} console.log('OK')" && pnpm -r test 2>&1 | grep -qE 'failed|FAIL' && exit 1 || echo 'OK'</automated>
  </verify>
  <acceptance_criteria>
    - `examples/tenant-app/docs/live-validation.md` records the chosen consume transport (fetch or navigation) with evidence and the CHIPS partition-isolation observation
    - `examples/tenant-app/lib/consume-transport.ts` is locked to the observed transport (no internal req-IDs)
    - `docs/threat-model.md` THREAT-06 honesty boundary is updated to cite the live observation as the manual-check evidence and to record the resolved consume transport; no existing THREAT-NN invariant is weakened
    - `pnpm -r test` exits 0 (workspace stays green after the seam lock)
  </acceptance_criteria>
  <done>The live observations are recorded, the consume-transport seam is locked to the observed transport, and the THREAT-06 honesty boundary in docs/threat-model.md cites the live CHIPS evidence — the release gate's manual evidence is captured and the workspace stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host-shell origin (top-level) -> embedded tenant-app iframe (CROSS-SITE, LIVE) | The real third-party context on two distinct *.vercel.app origins; this is where THREAT-06's CHIPS partition enforcement is finally observed in a real browser. |
| real Entra session -> KV-backed bridge/consume (serverless) | The live, production-shaped roundtrip; in-memory would fail by construction, so this proves the KV path. |
| consume 302 Set-Cookie -> browser cookie jar (partition commit) | The empirical D-10 question: does the Partitioned cookie commit to the embedding partition over the chosen transport? Observed and recorded here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-15 | Information Disclosure | cross-tenant cookie leakage | mitigate | CHIPS partitioning isolates the session cookie to the embedding top-level (host-shell) partition; the live DevTools check confirms a different host partition does NOT see the cookie, and the evidence is recorded as the THREAT-06 manual-check evidence. |
| T-05-16 | Tampering | hollow-gate regression | mitigate | The two deploy origins are asserted distinct (HOST_SHELL_ORIGIN != APP_ORIGIN) so the live test is genuinely cross-site; a same-origin shortcut cannot silently pass the gate. |
| T-05-17 | Spoofing | wrong consume transport masks behavior | mitigate | The transport is chosen from the LIVE observation (not hard-coded); if fetch does not commit the partitioned cookie, the seam is switched to navigation and re-observed before locking. |
| T-05-18 | Tampering | live secrets exposure | mitigate | All real secrets live in Vercel project env + the Upstash integration (D-04); nothing real is committed; the repo carries only .env.example placeholders. |

Note: this plan records the consume-transport decision and the live CHIPS evidence — a change touching consume/cookie behavior. Per CLAUDE.md threat-model discipline, docs/threat-model.md is updated (Task 3) and the relevant negative case (handle replay -> 4xx, no cookie) is already covered by the Plan 04 Keycloak roundtrip test and the existing package consume-route tests. No existing invariant is weakened.
</threat_model>

<verification>
- Two distinct Vercel origins deployed (tenant app + host-shell); HOST_SHELL_ORIGIN != APP_ORIGIN.
- The live cross-site popup roundtrip signs the iframe in across >=2 tenants against a real Entra registration (KV-backed).
- DevTools confirms the session cookie is Partitioned and isolated to the host-shell partition.
- The D-10 transport is chosen from the observation; the seam is locked to it.
- docs/threat-model.md THREAT-06 honesty boundary cites the recorded live evidence.
- `pnpm -r test` stays green throughout.
</verification>

<success_criteria>
- The reference example is deployed to two distinct *.vercel.app origins (genuinely cross-site) against a real multi-tenant Entra registration using the KV adapter, and the popup roundtrip works live across >=2 tenants.
- Real CHIPS partition isolation is observed in DevTools and recorded.
- The D-10 consume transport is chosen from the live observation and the seam is locked to it.
- docs/threat-model.md THREAT-06 honesty boundary is closed with the live evidence; the workspace test bench stays green.
</success_criteria>

<output>
Create `.planning/phases/05-multi-tenant-reference-example/05-05-SUMMARY.md` when done.
</output>
